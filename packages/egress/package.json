{
  "name": "@agent-security/egress",
  "version": "0.1.0",
  "description": "Egress control and DLP for Agent-SPM",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "keywords": ["agent", "security", "egress", "dlp", "spm"],
  "license": "MIT",
  "dependencies": {
    "@agent-security/core": "*",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.3.3"
  }
}
