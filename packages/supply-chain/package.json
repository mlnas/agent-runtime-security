{
  "name": "@agent-security/supply-chain",
  "version": "0.1.0",
  "description": "Tool supply chain security for Agent-SPM",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "ai",
    "agent",
    "security",
    "policy",
    "runtime",
    "llm",
    "agentic",
    "agent-spm",
    "compliance",
    "dlp"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/mlnas/agent-runtime-security.git"
  },
  "bugs": {
    "url": "https://github.com/mlnas/agent-runtime-security/issues"
  },
  "homepage": "https://github.com/mlnas/agent-runtime-security#readme",
  "dependencies": {
    "@agent-security/core": "*",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.3.3"
  }
}


SECTION 1 — Intent and Driver fixes
1. C-27 → "Not Implemented for MVP - see R-10". Also reword the Description: the designed
   control BOUNDS the bearer window (short expiry, 3 sessions, generation/use logged); it
   does not remove the bearer property that is S-7 ("there is no identity lookup" — edge
   DESIGN.md §3). Keep the template sentence shape; the row must not read as if S-7 is
   neutralised.
2. C-26 → "To confirm". The firewall and Flow Logs exist in LZA but both Suricata
   rulesFiles are absent from the checkout and no OCS-specific egress destination exists
   anywhere — the allow-list content cannot be evidenced.
3. C-13 Intent: delete "and Zendesk alert" (zendeskService.ts has zero callers).
4. C-17 → "Implemented" citing objectLockEnabled + ObjectLockRetention.compliance(14 days)
   + versioning + access logging (quarantineResources.ts).
5. C-32: upgrade to "Implemented" ONLY if you can verify operational service roles cannot
   modify/delete the CloudWatch log group as well as the trail bucket; otherwise keep
   "To confirm" but add the verified evidence (TEN_YEARS retention, trail-bucket
   DENY s3:DeleteObject* for * principals — auditLogs.ts) and a REVIEW NOTE naming exactly
   what remains unconfirmed.
6. C-42: keep "In scope for delivery" but append the prerequisite to the Intent: the LZA
   enables GuardDuty + S3 Protection only — no Malware Protection configuration exists, so
   the scan step's engine requires UKB org-level enablement first. Also fix its Driver:
   E-1 is the wrong proxy for T-13's escalation impact — cite only IDs that match.
7. C-3 Driver: remove E-8 (owned by C-36/C-38; matrix agrees C-3 is S-4/E-12).

SECTION 2 — restore the displaced control (the C-42 ID reuse)
8. The 13-07 register reinstated "C-42" as the interim content-review disclosure control
   (human review documented against output-checking criteria); that ID now means malware
   scanning. Add a NEW row (next free ID) for the interim human content review, status
   per current verified practice, Drivers derived and verified from THREAT_MODEL.md
   (HT-1/I-1 family).
9. Add a NEW row for automated content inspection (decision-engine integration, the
   post-MVP plan of record) marked "Not Implemented for MVP - see R-11". This is the #1
   threat from your coverage check — it must have a risk statement for UKB to accept.

SECTION 3 — Risks table fixes
10. R-1 Mitigation: remove the claims that cite C-19 and C-28 as in place (both are
    Not Implemented in §1 and both are R-1's own related design items) and remove the edge
    ADR020 re-verification claim unless you can verify edge code exists. Keep what
    survives verification: C-18 conditional-write state gating, C-6, C-30.
11. R-2 Mitigation: verify the ADR021 "bounded, allowlisted, audited presigned URLs"
    inspection claim against actual code (your repo-wide grep found zero presign hits).
    Cut or reword to what the code shows.
12. R-4: reword "Token TTL will be set" — the lifetime is a pure expires_in passthrough
    from the IdP (oidcIdPService.ts); no OCS-side bound exists and the value is
    unconfirmed (UKB/AMS question).
13. R-5 and R-7: remove the "raise a Zendesk alert" claims (zero callers).
14. R-7: remove "changes to the audit table and its policies are CloudTrail-logged with
    alerting" — no audit table exists.
15. R-8: replace the ADR007 audit-table evidence with what the code shows: PITR on both
    DynamoDB tables, versioning on all buckets, quarantine Object Lock (compliance, 14d),
    trail-bucket delete-deny, and RemovalPolicy.RETAIN gated on retainResources (prod
    only — say so). Remove the stack-termination-protection claim (zero hits repo-wide).
16. R-6: fix the correlation claim — the dl- leg belongs to the unbuilt edge zone, and the
    "append-only audit row" store is the CloudWatch/CloudTrail stack, not a table.
17. Add R-10 — bearer-link exposure: anyone holding the delivery link within its
    expiry/session budget obtains the object with no re-authentication and no possible
    network restriction; mechanism additionally not yet built. Related item C-27.
    Rating PROPOSED.
18. Add R-11 — no automated content inspection at MVP (single human reviewer reading
    requester-influenced metadata is the sole disclosure control). Related items: the two
    new rows from Section 2 (+ C-14 if verified). Mitigations only from in-place/committed
    items. Rating PROPOSED.
19. From your coverage check you also found I-6 unowned and I-12 claimed-but-uncovered —
    re-derive both from THREAT_MODEL.md and resolve them the same way (coverage citation
    fix, new row, or new R-row as the evidence dictates). Show your reasoning for each.

SECTION 4 — sibling files (same PR, small)
20. THREAT_MODEL.md §8: fix the present-tense claim that the inner-ingestion-zone SFN
    "runs a GuardDuty scan step" (nothing exists in code — planned per ADR018, org
    prerequisite missing) and update the half-stale gap-S2 note.
21. CONTROLS_MATRIX.md ends at C-41: add C-42 and the new rows, or REVIEW NOTE if the
    matrix format makes that non-trivial.

FINAL SELF-CHECK (run after editing, report pass/fail per item)
a. No Mitigation cell cites a control whose §1 Intent is "Not Implemented for MVP".
b. Every "Not Implemented" row points at an R-row that exists; every R-row's related
   design items are actually Not Implemented.
c. Every capture C-n / file / threat-ID citation resolves and says what the cell claims.
d. Intent column is strictly one of the four allowed values.
e. No FR-SEC refs in Driver; Decision column empty; all new ratings marked PROPOSED.

OUTPUT: (1) the change summary formatted for the PR description (including the C-42 ID
collision, the NFR-SEC numbering ruling already in the header, and the FR-SEC-012
deviation note), (2) REVIEW NOTES — everything you could not verify, (3) the self-check
results. Do not commit or raise the PR — I review first.
