{
  "name": "agent-runtime-security",
  "version": "0.1.0",
  "description": "Open-source SDK for adding runtime security policies to AI agents",
  "private": true,
  "scripts": {
    "install:all": "cd core && npm install && cd .. && npm install",
    "build": "cd core && npm run build",
    "demo": "npm run build && ts-node demo.ts",
    "demo:quick": "npm run build && ts-node test-demo.ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/uuid": "^10.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "uuid": "^9.0.1"
  }
}
